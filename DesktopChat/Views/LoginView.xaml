<UserControl x:Class="DesktopChat.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:DesktopChat.Views"
             xmlns:helper="clr-namespace:DesktopChat.Helpers"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">
            <ui:TextBlock FontTypography="Title" Text="Welcome back" TextAlignment="Center"  Margin="0,0,0,20" />
            <ui:TextBox 
                PlaceholderText="Email" 
                Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                Width="400" 
                Margin="0,0,0,20"  />

            <ui:TextBlock Text="Password" Margin="2,0,0,2" />
            <PasswordBox            
                Margin="0,0,0,20" 
                Width="400"
                helper:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay}"
    />

            <ui:Button Appearance="Primary" Content="Đăng nhập" Command="{Binding LoginCommand}" Margin="0,0,0,20"  Width="400"/>
            <ProgressBar 
                IsIndeterminate="True"
                Visibility="{Binding IsHidden, UpdateSourceTrigger=PropertyChanged}"/>

        </StackPanel>

    </Grid>

</UserControl>
